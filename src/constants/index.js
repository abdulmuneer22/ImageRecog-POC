import {Dimensions} from 'react-native';
const window = Dimensions.get('window');


export const Colors = {
    FADED_TEXT : 'rgb(38, 44, 47)'
}

export const Metrics = {
    FULL_WIDTH : window.width,
    FULL_HEIGHT : window.height
}